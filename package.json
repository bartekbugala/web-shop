{
  "scripts": {
    "start": "npm-run-all -p -l start:*",
    "start:server": "cd web-shop && cd server && yarn start",
    "start:client": "cd web-shop && cd client && yarn start"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
