{
  "scripts": {
    "start": "npm-run-all -p -l start:*",
    "install": "npm-run-all -p -l install:*",
    "start:server": "cd web-shop && cd server && yarn start",
    "start:client": "cd web-shop && cd client && yarn start",
    "install:server": "cd web-shop && cd server && yarn install",
    "install:client": "cd web-shop && cd client && yarn install"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
